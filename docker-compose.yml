services:
  discord-bot:
    build: .
    container_name: discord-human-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      # Persistir la memoria y jerga aprendida del bot
      - ./bot_memory.db:/app/bot_memory.db
